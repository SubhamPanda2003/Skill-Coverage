```python
def greet(name):
    """Return a greeting message for the given name.

    Args:
        name: The name of the person being greeted.

    Returns:
        A greeting message that includes the name.
    """
    greeting = f'Hello, {name}!'
    return greeting
```