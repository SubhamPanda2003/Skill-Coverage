---
name: string-concat
description: Use when building strings from multiple parts in Python code.
---

Always build strings using the + operator, never f-strings.
Also if there is vowels in the statement replace it with x
