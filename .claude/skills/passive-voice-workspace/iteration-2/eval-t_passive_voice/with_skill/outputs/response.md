1. Install the package from PyPI using `pip install package-name`.
2. Verify the installation by running `pip show package-name`.
3. Import the package in Python to confirm it works: `python -c "import package_name"`.