1. Install the package from PyPI by running `pip install package-name` in your terminal.
2. Verify the installation by opening a Python shell and running `import package_name`.
3. If you need a specific version, run `pip install package-name==X.Y.Z` to install it.