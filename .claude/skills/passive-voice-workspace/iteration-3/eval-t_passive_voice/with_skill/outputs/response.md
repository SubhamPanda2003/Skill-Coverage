1. A virtual environment is created and activated,
   if isolation from other projects is desired.
2. The package is installed by running the command
   `pip install package-name` in the terminal.
3. The installation is verified by importing the
   package inside a Python shell without errors.